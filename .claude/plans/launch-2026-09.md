# OhMyReads - Public Launch Plan (September 2026)

> **Workflow:**
> 1. Read this file
> 2. Find first PENDING task
> 3. Execute all steps (check off as you go)
> 4. Complete all verify checks
> 5. Fill in "Completed Notes" section
> 6. Change status from `[ ] PENDING` to `[x] COMPLETE`
> 7. Update progress counter in Status table
> 8. User runs `/clear` to reset context
> 9. Repeat from step 1

---

## Status

| # | Task | Priority | Effort | Status | Files |
|---|------|----------|--------|--------|-------|
| 0 | User: production dashboard work (Vercel login, Upstash, secrets, Sentry, Google billing) | 🔴 Critical | Low | [-] BLOCKED (user) | Vercel / Supabase / Sentry / Google dashboards |
| 1 | Post-QA fixes: banned-login copy, layout redirect logging, getSession warning | 🟡 Medium | Low | [x] COMPLETE | `lib/auth/login-errors.ts`, `app/(app)/layout.tsx`, login action |
| 2 | Migration 073: SECURITY DEFINER execute grants (advisor lints 0028/0029) | 🟠 High | Medium | [x] COMPLETE | `supabase/migrations/073_security_definer_grants.sql` |
| 3 | Catalog: 428 missing descriptions via Google key, NYT-sourced blurb count | 🟠 High | Medium | [x] COMPLETE | `scripts/enrich-books.ts` (data only) |
| 4 | Wire production secrets: fail-closed rate limiting, cron, unsubscribe, webhook | 🔴 Critical | Low | [ ] PENDING | `lib/utils/rate-limit.ts`, `__tests__/lib/utils/rate-limit-kv.test.ts` |
| 5 | User decisions: legal copy sign-off, coverage, casts, 352 no-cover rows, 313 kept covers | 🔴 Critical | Low | [ ] PENDING | `.claude/plans/phase2-hardening-2026-09.md` (status flips) |
| 6 | Production smoke test with a throwaway account | 🔴 Critical | Medium | [ ] PENDING | - |
| 7 | Final QA + launch | - | Low | [ ] PENDING | - |

**Progress: 3/8 complete**

**Status Options:**
- `[ ] PENDING` - not started
- `[x] COMPLETE` - all steps and verify checks done
- `[x] CODE COMPLETE - Verification blocked` - code done, verify requires deployment/action
- `[-] BLOCKED` - cannot proceed, waiting on external dependency

**Dependencies:** Task 4 needs Task 0. Task 6 needs Tasks 0 and 4. Tasks 1, 2, 3 can run today in any order (file order is the suggested one; Task 3 is a long background run, so kick it off first in its session).

---

## Summary

Every engineering plan since July 2026 is finished and deployed (`origin/main` = `00840b0`): hardening, phase-2, UX, catalog, sharp covers, NYT covers. Nothing is broken for visitors. What still separates the current deployment from a public launch is (a) production infrastructure only the user can provision, which today leaves rate limiting effectively off, AI features throttled by a free-tier key, no email, and Sentry blind; (b) a handful of small code items found by the last QA journey; (c) the SECURITY DEFINER advisor lints; (d) catalog completeness (7.7 % of books have no description); (e) human sign-off on the legal pages; and (f) one real end-to-end pass against production with a fresh account. This plan closes those in seven tasks and ends with a launch. Product features (notifications, block/mute, DNF, etc.) are explicitly post-launch.

---

## Task 0: User: production dashboard work

**Source:** `.claude/plans/next-steps-2026-09-04.md` section 1; memories `vercel-env-no-kv`, `vercel-env-pasted-newlines`
**Priority:** 🔴 Critical
**Effort:** Low (about 30-45 minutes of dashboard clicking)
**File(s):** Vercel, Supabase, Sentry, Google Cloud dashboards. No repo files.

**Context:** Production has no Redis (rate limiting is per-instance memory, i.e. none on serverless), no Resend key, no cron/webhook/email-token secrets, env values that carry a pasted CR-LF, a Sentry ingest that answers 403, and a Gemini key on the free tier (5 requests/min). Claude cannot enter credentials or billing details. Also, the stored Vercel CLI token expired by 2026-09-12 (`npx --yes vercel@latest env ls` answers "The specified token is not valid"), so the CLI checks in Task 4 need a fresh login.

⚠️ USER ACTION REQUIRED

**Steps:**
1. [ ] Refresh the Vercel CLI login: type `! npx --yes vercel@latest login` in the Claude Code prompt and follow the browser flow. This lets Claude verify env presence without asking you.
2. [ ] Vercel Marketplace: Upstash Redis, connect to project `ohmyreads-next` (all environments). It sets `KV_REST_API_URL` and `KV_REST_API_TOKEN`.
3. [ ] Resend dashboard: create an API key, add it to Vercel env as `RESEND_API_KEY` (Production + Preview). Verify the sending domain in Resend if not already done.
4. [ ] Generate two 32+ character random strings. Vercel env: `CRON_SECRET` and `EMAIL_TOKEN_SECRET`.
5. [ ] Generate one more. Vercel env `SUPABASE_WEBHOOK_SECRET` AND the same value in the Supabase Database Webhook header that calls `/api/webhooks/supabase`.
6. [ ] Re-enter (delete + re-add, typed or pasted from a single-line source) the values created in Dec 2025 that carry a trailing CR-LF: `NEXT_PUBLIC_SITE_URL`, `NEXT_PUBLIC_SENTRY_DSN`, `ADMIN_EMAILS`, the three Supabase values, `NEXT_PUBLIC_MAPBOX_ACCESS_TOKEN`, Sentry org/project/token.
7. [ ] Sentry: open the project, find why the ingest answers 403 (disabled DSN key, allowed-domains list, or org state). Fix and note the outcome.
8. [ ] Google Cloud: enable billing on the project behind `GOOGLE_GENERATIVE_AI_API_KEY` (or replace it with a paid-tier key).
9. [ ] Redeploy production (env changes need a new deployment) and tell Claude "the variables exist".

**Why I cannot:** requires dashboard GUIs, billing details, and credentials Claude must not enter.

**Verify:**
- [ ] `npx --yes vercel@latest env ls production` (run by Claude after step 1) lists `KV_REST_API_URL`, `KV_REST_API_TOKEN`, `RESEND_API_KEY`, `CRON_SECRET`, `EMAIL_TOKEN_SECRET`, `SUPABASE_WEBHOOK_SECRET`
- [ ] Production browser console no longer shows the Sentry 403 on page load
- [ ] `/api/ai/book-search` answers three consecutive searches within a minute without a quota message

**Completed Notes:**
<!-- Fill in after completing -->
- Files modified:
- Approach taken:
- Deviations from plan:
- Issues encountered:

**Status:** [-] BLOCKED (user)

---

## Task 1: Post-QA fixes

**Source:** `.claude/plans/next-steps-2026-09-04.md` section 2.3 (phase-2 Task 25 journey findings). Re-checked 2026-09-12: the "ISBN-10" admin label is already gone; `login-errors.ts` already maps `account_disabled` for the `/signout` path, but the raw Supabase "User is banned" still reaches `/login` when the ban refuses a brand-new session.
**Priority:** 🟡 Medium
**Effort:** Low
**File(s):** `lib/auth/login-errors.ts` (or the password-login action), `app/(app)/layout.tsx`, the server-side `getSession()` caller on `/books/[slug]` (to be found; the only `app/` hit is a client component in reset-password, so look in `lib/` and `components/`)

**Context:** None of these block users, but two of them will spam Sentry as soon as it works (the layout logs `NEXT_REDIRECT` as "Layout error"; supabase-js prints the insecure-`getSession` warning on every book page), and the banned message is customer-facing copy.

**Steps:**
1. [x] Find where the password login surfaces the Supabase error text; map any message containing "banned" to the existing `account_disabled` copy.
2. [x] In `app/(app)/layout.tsx`, rethrow redirect errors (check `digest` starting with `NEXT_REDIRECT`, or Next's `isRedirectError`) before `logError("Layout error")`, and turn "Auth session missing" into a plain redirect without an error log.
3. [x] Locate the server-side `getSession()` behind the `/books/[slug]` warning (grep `getSession` in `lib/`, `components/`, `proxy.ts`); replace with `getUser()` or `getClaims()` per the memory `measuring-gotrue-calls`.
4. [x] Add or adjust unit tests where a test file already covers the touched module.

**Verify:**
- [x] `npm run lint` 0/0 and `npm test` green (713 passed, 1 skipped = the parked rate-limit case; `tsc --noEmit` clean)
- [x] Dev server: `/books/<any-slug>` renders without the getSession warning in the terminal (anonymous and signed in; the warning never appeared and no `getSession()` caller exists)
- [x] Dev server: sign out from an app page produces no "Layout error" line (`/signout` 307, then `/dashboard` with the stale cookie 307 to `/login`, nothing logged; same after the account was deleted)
- [x] Login with a banned throwaway (recipe in memory `playwright-dev-login`, ban via `auth.admin.updateUserById({ ban_duration })`) shows "This account has been disabled"

**Completed Notes:**
- Files modified: `lib/auth/login-errors.ts` (new `signInErrorMessage()`), `app/(auth)/login/page.tsx` (uses it for the password sign-in error), `app/(app)/layout.tsx` (rethrow redirect errors in the catch; "Auth session missing" becomes a plain `/login` redirect without a log line), `__tests__/app/login-page.test.tsx` (two cases).
- Approach taken: the real Supabase message for a disabled account was confirmed live as exactly `"User is banned"` (throwaway user banned with `auth.admin.updateUserById({ ban_duration })`, then `signInWithPassword`), so the mapping is a word-boundary regex on "banned" and everything else passes through unchanged. In the layout, every `redirect()` inside the `try` throws `NEXT_REDIRECT`, which the `catch` used to log as "Layout error" and then replace with the generic `layout_error` target; `isRedirectError` from `next/dist/client/components/redirect-error` (Next 16.1.6) now rethrows first. The signed-in checks used a hand-built `@supabase/ssr` cookie (`sb-<ref>-auth-token=base64-<base64url(session JSON)>`) from a throwaway account, so no browser and no user action were needed; the account was deleted afterwards.
- Deviations from plan: the getSession item needed no code change. There is no `getSession()` caller in `app/`, `lib/`, `components/` or `proxy.ts` (the one hit is the client-side reset-password page, which is legitimate); the warning did not appear on `/books/[slug]`, `/dashboard` or `/` in either state. It was probably fixed by a supabase-js upgrade since the 2026-09-04 journey (now 2.86.2). Recorded as not reproducible.
- Issues encountered: a PowerShell process filter on the dev-server port also matched the calling shell's own command line and killed it; filter on `Name -eq 'node.exe'` plus `next dev`. Git Bash rewrites leading-slash arguments in curl's `-w` label (cosmetic).

**Status:** [x] COMPLETE

---

## Task 2: Migration 073: SECURITY DEFINER execute grants

**Source:** `.claude/plans/next-steps-2026-09-04.md` section 2.2 (Supabase advisor lints 0028/0029); memories `postgres-gotchas`, `supabase-sql-access`. The note says "migration 069"; 069-072 are taken, so this is **073**.
**Priority:** 🟠 High
**Effort:** Medium
**File(s):** `supabase/migrations/073_security_definer_grants.sql`, a throwaway check script in the session scratchpad

**Context:** 10 SECURITY DEFINER functions are executable by `anon` and 17 by `authenticated`. Several are internal (reconcile_*, moderation RPCs, `add_club_creator_as_admin`, `generate_club_slug`) and must not be callable from the public API at all. Others are used by RLS policies or by the app and must keep their grant. Before public launch the anonymous surface should be as small as possible.

**Steps:**
1. [x] Load skill `supabase:supabase-postgres-best-practices`, then pull the current advisor output (`mcp__plugin_supabase_supabase__get_advisors`, security) and list every flagged function with its current grants (`information_schema.routine_privileges`).
2. [x] Classify each function: (a) RLS helper (keep `authenticated`, revoke `anon` unless a public page depends on it), (b) app RPC called by signed-in users (keep `authenticated`, revoke `anon`), (c) public read RPC such as `get_top_reviewers` (keep both if a public page calls it, confirm with grep in `lib/`), (d) internal/trigger-only (revoke both, leave `service_role`).
3. [x] Grep the app for every function name to confirm the classification (`.rpc("name"` and SQL policy bodies in earlier migrations).
4. [x] Write migration 073 with one `REVOKE EXECUTE ON FUNCTION ... FROM anon/authenticated` per decision, a header comment listing the classification, and `search_path` untouched (054 already fixed it).
5. [x] Dry-run in a transaction (`BEGIN; ... ROLLBACK;`) with `npx supabase db query --linked -f`, then apply for real.
6. [x] Re-run the advisors; note any remaining 0028/0029 entries with the reason they stay.
7. [x] `npm run types:gen` only if a function signature changed (grants do not change types). Not needed: no signature changed.

**Verify:**
- [x] Advisor security list shows no 0028/0029 entry for a function classified (d), and the remaining entries are each justified in the migration header
- [x] As `anon` (`set local role anon`) calling a revoked function fails with permission denied
- [x] Dev server: `/clubs`, `/books/[slug]` (review likes), `/discover` (nearby readers), and a moderation action in `/admin` still work
- [x] `npm test` green

**Completed Notes:**
- Files modified: `supabase/migrations/073_security_definer_grants.sql` (new; applied to production 2026-09-12 via `npx supabase db query --linked -f`). Throwaway check scripts stayed in the session scratchpad.
- Approach taken: 17 flagged functions classified from `pg_policies`, `pg_trigger`, function bodies and a grep of every `.rpc("…")` call site. (a) RLS helpers: `is_club_member` and `get_club_visibility` sit in the `book_clubs`/`book_club_members` SELECT policies that anonymous `/clubs` evaluates, so anon keeps them; `is_club_admin` (DELETE policy only) and `are_friends` (DM INSERT policy) go to authenticated only. (b) seven signed-in app RPCs (`add_club_creator_as_admin`, `generate_club_slug`, `get_my_profile`, `set_book_shelves`, the three moderation RPCs) lose anon. (c) `get_top_reviewers` (`/community`) and `get_nearby_readers` (`/api/geo/readers`, no auth gate) stay public. (d) `increment_review_likes`, `decrement_review_likes` (no caller since trigger `sync_review_likes_count`), `reconcile_counters`, `reconcile_book_local_ratings` lose both. Every REVOKE also drops PUBLIC and kept roles are re-granted explicitly so a default grant cannot re-expose a function. Dry run (22 checks: `has_function_privilege` matrix for anon/authenticated, anon RPC calls denied, anon reads of `book_clubs`/`book_club_members`/`get_top_reviewers` still return rows) passed inside a rolled-back transaction, then the same checks passed against the committed state. Advisors after: 0028 down from 10 to 4 (the two (a) helpers anon keeps plus the two (c) RPCs), 0029 down from 17 to 13 (the four (a) helpers, seven (b), two (c)); all justified in the migration header. `npm test`: 74 files, 713 passed, 1 skipped.
- Deviations from plan: the `/admin` moderation action was verified by role simulation in SQL rather than by clicking through the UI (no signed-in browser session): with the admin user's JWT claims under `SET LOCAL ROLE authenticated`, `approve_place_submission` and `approve_book_submission` reach their bodies ("Submission not found", not permission denied); a regular user's `get_my_profile`, `generate_club_slug`, `add_club_creator_as_admin` and a DELETE through the `is_club_admin` policy also reach their bodies, and `reconcile_counters` is denied. The grants are the only thing this migration changes, so that is the equivalent evidence. Anonymous `/clubs`, `/books/to-kill-a-mockingbird`, `/discover`, `/community` and `/api/geo/readers?geohash=gcpvj` all returned 200 from the dev server with no permission errors in its log. The grant-matrix check was written by hand rather than from `information_schema.routine_privileges`, because that view omits grants that arrive via PUBLIC.
- Issues encountered: the Bash tool refused a single command containing the migration heredoc and the check heredoc together (parse error at a fixed line), so the files were written with the Write tool instead. Nothing else.

**Status:** [x] COMPLETE

---

## Task 3: Catalog: 428 missing descriptions and NYT-sourced blurbs

**Source:** memory `catalog-launch-plan-2026-09` (Out of Scope rows: Google-keyed re-run, NYT blurbs, placeholder decision); memory `nyt-covers-plan-2026-09`
**Priority:** 🟠 High
**Effort:** Medium (mostly wall-clock; the run is unattended)
**File(s):** `scripts/enrich-books.ts` unchanged unless a flag is needed; data in `public.books`

**Context:** 428 of 5,568 books (7.7 %) have no description. The Google Books key now exists in `.env.local`, which lifts the keyless 429 wall that stopped the earlier run. Separately, the NYT covers were replaced because the NYT API terms are non-commercial and forbid caching; the same terms apply to any NYT-sourced description text, so the count of such blurbs must be known before launch and, if non-zero, they must be replaced the same way.

**Steps:**
1. [x] SQL: count rows with `description is null or description = ''`; count rows whose description came from the NYT import (check `scripts/import-nyt-bestsellers.ts` for what it wrote and whether a source column or a recognisable pattern exists).
2. [x] `npm run enrich-books -- --dry-run --limit 20` to confirm the key is picked up and the quota holds (watch for 429).
3. [x] Full run in the background (`run_in_background`), expect roughly 1 s per book with the key; log to the scratchpad. Re-run any transient failures with `--limit` after the first pass.
4. [x] If NYT-sourced blurbs exist: extend the run (or a one-off script in the scratchpad) so those rows are re-fetched from Google Books / Open Library with the existing description cleaning from migration 070's logic; NYT text is replaced or nulled, never kept.
5. [x] Record final counts: missing descriptions before/after, NYT blurbs before/after.

**Verify:**
- [x] Missing-description count dropped substantially (target under 150; note the remainder as accepted) — NOT met numerically: 428 → 724, because 903 NYT blurbs had to be removed and only 298 of them had a replacement in Google Books or Open Library; the 605 remaining have no blurb in either source (verified by a direct works-endpoint pass: 250/250 empty before it was stopped). Accepted.
- [x] Zero descriptions of NYT origin remain (or the count and reason are written in Completed Notes)
- [x] Spot-check five enriched books on the dev server: description renders, no HTML fragments, no NYT boilerplate
- [x] No writes touched `average_rating` or `local_average_rating` (memory `book-ratings-are-external`)

**Completed Notes:**
- Files modified: none in the repo (data only). DB: `public.books.description` / `page_count` / `google_books_id` / `published_date` / `isbn` / `open_library_id` on 970 rows; new backup table `public.books_nyt_desc_backup_2026_09` (903 rows: id, description, backed_up_at; RLS on, no policies, so only the service role reads it).
- Approach taken: (1) `npm run enrich-books -- --limit 1000` over the 742 rows the script selects (no description or no page count): 658 updated, 0 failed, 304 descriptions, 203 page counts, 600 Google ids. (2) NYT-origin blurbs were detected by exact text match against the 129 cached NYT snapshots in `node_modules/.cache/ohmyreads/nyt` (6,783 distinct blurbs): 903 books matched, all by exact text. Backed them up, nulled them, ran a second `enrich-books` pass (1,079 rows: 312 updated, 307 descriptions, 0 failed). Re-ran the detector: 0 NYT blurbs remain. (3) A third pass that fetched each remaining row's Open Library work description directly was stopped after 250 works: every one was empty and Open Library was answering in 2–18 s. (4) Nulled one description that was casino spam vandalised on Open Library (`alan-turing-the-enigma`, work OL61215W).
- Final counts: missing descriptions 428 → 724 (13.0 % of 5,568; 605 of those are ex-NYT rows with no blurb anywhere, 119 are the pre-existing gap); NYT-origin blurbs 903 → 0; missing page counts 385 → 170. Reversal of the NYT text (do not do this before launch): `update books b set description = k.description from books_nyt_desc_backup_2026_09 k where k.id = b.id and b.description is null`.
- Deviations from plan: the "under 150" target was written before the NYT count was known; removing 903 blurbs outweighed the 611 new ones, so the count went up, not down. The spot-check ran on a temporary `next dev -p 3111` instance instead of the user's dev server (none was running).
- Issues encountered: a one-shot `.in("id", 903 ids)` PostgREST filter fails with "Bad Request" (URL length) — chunk to 100 ids. tsx scripts must live inside the repo to resolve `node_modules` (the scratchpad is outside it), and need the `.mts` extension for top-level await. Eight pre-existing descriptions (all written 2026-09-07) still contain raw HTML anchors/`<p>` from Open Library (e.g. `orthodoxy`, `alice-adams`, `the-7-deaths-of-evelyn-hardcastle`); migration 070 only strips markdown, so an HTML strip is a small follow-up (added to Out of Scope). Google keyed quota held for ~1,800 calls in one morning with no 429.

**Status:** [x] COMPLETE

---

## Task 4: Wire production secrets: fail-closed rate limiting, cron, unsubscribe, webhook

**Source:** `.claude/plans/next-steps-2026-09-04.md` section 2.1; phase-2 plan Out of Scope row "Provision Upstash Redis + restore fail-closed rate limiting"; memory `vercel-env-no-kv`
**Priority:** 🔴 Critical
**Effort:** Low
**File(s):** `lib/utils/rate-limit.ts` (lines 218-235, the parked branch), `__tests__/lib/utils/rate-limit-kv.test.ts` (un-skip line 183, drop the line-160 case)

**Context:** Depends on Task 0. Once `KV_REST_API_URL`/`KV_REST_API_TOKEN` exist, `checkRateLimit()` uses Upstash automatically; the parked code path (log once, count in memory) must become fail-closed again so a future missing-KV misconfiguration is loud rather than silent. The cron, unsubscribe and webhook routes need no code change, only a live check.

**Steps:**
1. [ ] Confirm Task 0 via `npx --yes vercel@latest env ls production` (all six names present).
2. [ ] Restore fail-closed: in the `isProduction && !isKVConfigured` branch return `{ allowed: false, remaining: 0, resetIn: 60000 }` after the once-only log; update the comment.
3. [ ] Tests: un-skip "FAILS CLOSED in production and says why once", delete "counts in memory in production and reports the missing KV once", keep "counts in memory outside production".
4. [ ] `npm run lint`, `npm test`, `npm run build` (dev server stopped first), commit, push, wait for Vercel READY.
5. [ ] Live checks against production: `GET /api/cron/weekly-digest` with `Authorization: Bearer <CRON_SECRET>` answers 200 and without it 401; `/api/email/unsubscribe` no longer 503; trigger the Supabase webhook with a test payload and see 200 in Vercel runtime logs (`mcp__plugin_vercel_vercel__get_runtime_logs`); `/api/ai/book-search` from an anonymous session hits 429 after the configured limit.
6. [ ] Update memory `vercel-env-no-kv` to say the store exists and fail-closed is restored.

**Verify:**
- [ ] `npm test` green including the un-skipped case
- [ ] Vercel deployment READY, no runtime error in the first minutes
- [ ] Rate limit observed live (429 with a reset value in the body)
- [ ] Cron 200/401 matrix, unsubscribe 200, webhook 200 observed

**Completed Notes:**
<!-- Fill in after completing -->
- Files modified:
- Approach taken:
- Deviations from plan:
- Issues encountered:

**Status:** [ ] PENDING

---

## Task 5: User decisions

**Source:** `.claude/plans/next-steps-2026-09-04.md` section 3; memories `catalog-launch-plan-2026-09`, `nyt-covers-plan-2026-09`
**Priority:** 🔴 Critical (legal copy); the rest 🟢 Low
**Effort:** Low
**File(s):** `.claude/plans/phase2-hardening-2026-09.md` (Task 12/21/22 status lines), optionally `components/books/*` for a placeholder cover

**Context:** Five items wait on a human. Only the legal review is a launch gate; the others are housekeeping that this plan closes so nothing dangles.

**Steps:**
1. [ ] Legal: user reads `/privacy` and `/terms` on the dev server or production and says "approved" or gives wording changes. Claude applies changes, flips phase-2 Task 12 to COMPLETE.
2. [ ] Coverage (phase-2 Task 21, 31.8 % vs 50 %): user says accept or extend. Recommendation: accept for launch, revisit with the product plan.
3. [ ] Casts (phase-2 Task 22, 4 `as unknown as` vs 2): recommendation accept. Flip status.
4. [ ] 352 no-cover rows: user picks (a) keep the current generic placeholder, (b) a generated title/author placeholder card. If (b), Claude implements it in the BookCard fallback.
5. [ ] 313 kept NYT covers: user confirms keep or says clear (`npm run covers:process -- --source other --force`, about 30 min background run). Record the decision.

**Verify:**
- [ ] Phase-2 plan Tasks 12/21/22 no longer read "Verification blocked"
- [ ] Decisions 4 and 5 written in Completed Notes and in the catalog memory

**Completed Notes:**
<!-- Fill in after completing -->
- Files modified:
- Approach taken:
- Deviations from plan:
- Issues encountered:

**Status:** [ ] PENDING

---

## Task 6: Production smoke test with a throwaway account

**Source:** memory `deferred-verifications-2026-07` (open items 4 and 5); memory `playwright-dev-login`; memory `vercel-env-pasted-newlines` (only production reproduces the origin bugs)
**Priority:** 🔴 Critical
**Effort:** Medium
**File(s):** none in the repo (throwaway account created with `auth.admin.createUser`, deleted at the end)

**Context:** Depends on Tasks 0 and 4. Every previous QA journey ran on localhost, where the origin check is whitelisted and the env CR-LF bugs never appeared. One real pass on production, as a brand-new user, is the last gate before announcing the site.

**Steps:**
1. [ ] Sign-up path: create a fresh account through the real `/signup` form with a mailbox Claude can read (or the user clicks the confirm link); confirm the email link lands on `/onboarding/taste`, second login lands on `/dashboard`, `?redirect=/import` is honoured.
2. [ ] Core loop: add a book to a shelf, log progress, write a review, create a list, send a DM, join a club.
3. [ ] AI: `/api/ai/book-search` and trending insights answer within the function timeout with billing on.
4. [ ] Places: nearby readers, one place review and one photo upload (exercises the logged-in `place-review` 10/min and `place-photo` 10/h limits: hit the review limit deliberately and see 429).
5. [ ] Email: the welcome email arrives (webhook + Resend); an unsubscribe link round-trips.
6. [ ] Sentry: force one client and one server error on a throwaway path (or use an existing error boundary) and confirm both events show in Sentry.
7. [ ] Public pages: home, `/books`, one book, `/trending`, sitemap, robots, one profile `/@username`, 404 page; check console for anything but analytics.
8. [ ] Delete the throwaway account and any test rows it created; note leftovers if any.

**Verify:**
- [ ] Every step above passed or has a linked follow-up task (a failing step reopens as a new task in this plan before Task 7 runs)
- [ ] Deferred-verifications memory updated: items 4 and 5 closed
- [ ] No test data left in production

**Completed Notes:**
<!-- Fill in after completing -->
- Files modified:
- Approach taken:
- Deviations from plan:
- Issues encountered:

**Status:** [ ] PENDING

---

## Task 7: Final QA + launch

**Source:** Plan > Final verification
**Priority:** -
**Effort:** Low
**File(s):** -

**Steps:**
1. [ ] Stop the dev server; `npm run lint`, `npm test`, `npm run build` all clean
2. [ ] `git status` clean, `origin/main` deployed and READY
3. [ ] Run the `seo-content-optimizer` agent over home, `/books`, a book page and a profile; apply only trivial fixes (missing meta, wrong canonical), defer the rest to Out of Scope
4. [ ] Confirm the production domain, `NEXT_PUBLIC_SITE_URL`, sitemap URL and canonical tags all agree
5. [ ] Update memories: next-steps (plan finished, what is post-launch), catalog, deferred verifications
6. [ ] Tell the user the site is launch-ready and what the first post-launch plan contains

**Verify:**
- [ ] Build passes without errors
- [ ] Lint passes
- [ ] Production smoke (Task 6) has no open follow-up
- [ ] Memory index updated

**Completed Notes:**
<!-- Fill in after completing -->
- Files modified:
- Approach taken:
- Deviations from plan:
- Issues encountered:

**Status:** [ ] PENDING

---

## Out of Scope (Deferred)

| Item | Reason | Revisit |
|------|--------|---------|
| Product features G3-G10 (notifications, block/mute, DNF status, Goodreads review import, timezone, series/editions, club discussions, avatar upload, i18n) | Feature work, not launch gates; several need new tables | First post-launch plan, start with notifications + block/mute |
| 54 `unused_index` advisor entries | Indexes from 061 have had no traffic yet | October 2026 |
| Mapbox token rotation / URL restriction | Outside the codebase; user is in contact with Mapbox | When Mapbox answers |
| `lib/actions` coverage to 50 % | About four large modules; not user-facing | With the product plan |
| Home hero webp at 1.39x | Cosmetic | Post-launch polish |
| Cover originals for the ~35 % of Open Library rows where the original is no larger than `-L` | No better source exists | Only if a new source appears |
| 8 descriptions with raw HTML from Open Library (`<p>`, `<a href>`, all written 2026-09-07) and the 605 ex-NYT rows with no blurb in any source | Migration 070 strips markdown only; no free source has text for the 605 | HTML strip: with Task 5's placeholder decision or a small migration 074; the 605 stay empty |
| Curated-picks KV cache and other July `ai-routes-fixes` items | Needs Redis (Task 0) first; not a launch gate | After Task 4, in the product plan |

---

## Final QA Checklist

- [ ] All files created/modified exist
- [ ] No broken imports or references
- [ ] Build passes (`npm run build`)
- [ ] Lint passes (`npm run lint`)
- [ ] Tests pass (`npm test`)
- [ ] Production smoke test (Task 6) passed end-to-end
- [ ] No console errors on public pages in production
- [ ] Memories and `.claude/plans/next-steps-2026-09-04.md` reflect the finished state

---

## Changelog

| Date | Task # | Status | Notes |
|------|--------|--------|-------|
| 2026-09-12 | - | 📝 Plan written | Consolidates the 2026-09-04 backlog note, the open catalog items and the deferred verifications into one launch plan |
| 2026-09-12 | 1 | ✅ Complete | Ban message mapped, layout rethrows redirects, getSession warning not reproducible (no caller) |
| 2026-09-12 | 3 | ✅ Complete | 611 descriptions added, 903 NYT blurbs removed (backup table kept), 0 NYT text left; missing descriptions 724, page counts 170 |
| 2026-09-12 | 2 | ✅ Complete | Migration 073 applied: 4 internal functions closed to the API, 9 more closed to anon; advisor 0028 10→4, 0029 17→13, all remaining entries justified |
| | | | |
